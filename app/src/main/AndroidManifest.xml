<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

        <uses-permission android:name="android.permission.INTERNET"/>
        <uses-permission android:name="android.permission.VIBRATE" />
        <!-- Required for Nearby Connections -->

        <!--    Because ACCESS_FINE_LOCATION is a dangerous permission, the app will have to-->
        <!--    request it at runtime, and the user will be prompted to grant or deny access.-->

        <uses-permission android:name="android.permission.BLUETOOTH" />
        <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
        <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

        <!-- Needed only if your app makes the device discoverable to Bluetooth
             devices. -->
        <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

        <!-- Needed only if your app communicates with already-paired Bluetooth
             devices. -->
        <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Buffalo_MoBill"
        tools:targetApi="31">
        <activity
            android:name=".Home"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Multiplayer"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".ClientConnexion"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".ServerConnexion"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".ScorePage"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".GameList"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".QuickQuiz"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".ShadyShowdown"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".PricklyPicking"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".CowCatcher"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".WildRide"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".MilkMaster"
            android:exported="true"
            android:theme="@style/Theme.Buffalo_MoBill"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>